spring:
  application:
    name: db-service

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: https://ims-db-service.herokuapp.com/eureka/
  instance:
    hostname: ims-discovery-server.herokuapp.com